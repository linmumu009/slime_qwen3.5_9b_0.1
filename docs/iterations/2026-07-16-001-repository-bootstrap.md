# 迭代 001：仓库初始化与迭代记录规范

- 日期：2026-07-16
- 状态：完成
- 初始化提交：`a350631d868f2fc8fcfb5afd5b496d951674f082`

## 迭代目标

为 `slime_qwen3.5_9b_0.1` 建立可持续维护的 Git 仓库基础，并把“README 写摘要、独立文件写完整说明”的迭代要求固化为仓库规则。

## 变更内容

1. 新建项目 README，说明仓库用途、迭代记录规则和安全要求。
2. 在 README 中建立迭代摘要表，每次迭代新增一行并链接到完整说明。
3. 新建 `docs/iterations/`，提供统一模板和本次初始化的完整说明。
4. 新建根目录 `AGENTS.md`，要求后续自动化开发任务把迭代文档与实现放在同一个提交中。
5. 新建 `.gitignore`，排除本地 Codex 状态、SSH 连接说明、环境变量文件和常见临时文件。
6. 将本地默认分支设为 `main`，并关联远端仓库 `https://github.com/linmumu009/slime_qwen3.5_9b_0.1.git`。

## 验证结果

- 已确认远端仓库可访问且当前没有提交。
- 已确认本地原有 `.git` 目录为空，并非有效 Git 仓库。
- 已检查忽略策略：`SSH_PRIVATE_KEY_LOGIN_*.md` 不会进入待提交列表。
- 已创建初始化提交 `a350631d868f2fc8fcfb5afd5b496d951674f082`，包含 5 个规范文件。
- 已将 `main` 推送到 `origin`，并确认本地 HEAD 与远端 `main` 哈希完全一致。

## 风险与兼容性

- SSH 连接说明仅保存在本机，不会随 Git 在其他环境中自动同步。
- `.env` 和本地凭据被忽略；部署或复现实验时应通过安全渠道单独配置。
- 迭代编号是仓库级递增编号；同一天发生多次迭代时继续递增，不重新从 001 开始。

## 回滚方式

如需撤销本次规范，可回退首次提交。远端仓库仍为空时，也可删除本地 Git 元数据后重新初始化；不得删除本机 SSH 私钥或连接说明。

## 后续事项

1. 在下一次功能迭代中复制模板并使用编号 002。
2. 随项目代码进入仓库，补充实际运行、依赖安装、训练和验证说明。
